import { Button } from "@/components/ui/button";

function App() {
  return (
    <div className="h-screen w-full gap-2 bg-background flex flex-col items-center justify-center">
      <h1 className="text-foreground">Financer</h1>
      <Button variant="default">Hi</Button>
    </div>
  );
}

export default App;
